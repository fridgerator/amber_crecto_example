secret_key_base: Yf8Zb_H_l71DjGvTWBP_Zr_rZMnQvxaF6mpSHVNYWyg
port: 3000
name: amber_crecto
log: "::Logger.new(STDOUT)"
log_level: "::Logger::INFO"
host: 0.0.0.0
ssl_key_file:
ssl_cert_file:
redis_url: "redis://localhost:6379"
session:
  key: amber.session
  store: :signed_cookie
  expires: 0
secrets:
  description: Store your development secrets credentials and settings here.
  database: postgres://postgres:@pg:5432/amber_crecto_development
